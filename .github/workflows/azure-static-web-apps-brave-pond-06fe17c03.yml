name: Azure Static Web Apps CI/CD (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled; use the Azure Web App workflow instead."
